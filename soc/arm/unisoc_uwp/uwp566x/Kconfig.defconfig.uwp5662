# Kconfig.defconfig.uwp5662
#

if SOC_UWP5662

config SOC
	string
	default "uwp5662"

config NUM_IRQS
	int
	# must be >= the highest interrupt number used
	default 179

config SYS_CLOCK_HW_CYCLES_PER_SEC
	int
	default 416000000

endif # SOC_UWP5662
